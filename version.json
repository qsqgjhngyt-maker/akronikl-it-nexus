# Manual smoke — v0.1.0-alpha.1

1. Open GitHub Pages in normal browser and private window.
2. Switch UI RU → EN → RU.
3. Independently change Course and Akronikl language selectors.
4. Search catalog and switch category chips.
5. Open C++ and verify 6 modules / 40 topics.
6. If legacy C++ was used on the same origin, verify detection → import → page reload → same progress.
7. Verify old `cpp_programming_pdf_course_v1` remains in localStorage after import.
8. Go offline after one online load and reopen shell.
9. After publishing a newer alpha, verify the new cache version appears without reinstalling PWA.
