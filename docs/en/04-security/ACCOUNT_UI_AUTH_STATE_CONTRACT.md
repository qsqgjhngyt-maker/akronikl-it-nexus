# Account UI Contract

Global signed-in header shows avatar, Nexus nickname and sync status.

Profile menu includes profile, learning, projects, skill map, devices/sessions, security, notifications and logout.

Auth state and Cloud Sync state are displayed separately. Raw session credentials are never shown.
