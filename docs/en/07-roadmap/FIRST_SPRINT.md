> **Documentation language policy:** Russian is the normative source. This English version is a secondary mirror for international users. If wording diverges, the Russian version prevails.

# First Sprint After Foundation

Automate v7 baseline tests, extract lesson/practicum/project/source data from the monolithic HTML without changing UI, introduce stable content IDs with progress migration, implement the new lesson schema for three reference topics, and define Akronikl’s context JSON contract without network AI. Done means no progress/UI regression, three lessons pass the new audits, baseline tests stay green and no client-side secret is introduced.
