# ADR-0016 — AI research precedes provider lock-in

**Status:** Accepted

Nexus AI core uses a provider abstraction. No single external LLM defines the architecture.

Every experiment records provider/model/config. Thesis claims should concern the intelligent system, not accidental behavior of one vendor model.
