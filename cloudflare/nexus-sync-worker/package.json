{
  "name": "akronikl-nexus-sync-worker",
  "private": true,
  "version": "0.1.7-alpha.2.4.2",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply akronikl-nexus-sync --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
