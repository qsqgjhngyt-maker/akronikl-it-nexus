# Architecture delta v0.1.1-alpha.1

`cpp-course` remains immutable reference material. Nexus now has a standalone root shell, core runtime, course registry, storage/migration layer, course package data and language registry. C++ content lives under `courses/cpp/data/`; future courses can use the same contract. UI language, course language and Akronikl language remain separate preferences.
