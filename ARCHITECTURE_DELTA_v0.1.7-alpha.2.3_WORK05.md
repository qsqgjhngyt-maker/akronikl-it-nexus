# Architecture Delta — `v0.1.7-alpha.2.3` WORK05

Runtime behavior unchanged.

Target architecture now defines:
- Nexus Identity Service/Broker;
- external identity adapters;
- revocable session/device model;
- passkey/MFA/recovery layer;
- security audit;
- auth abuse/rate-limit layer;
- first-party deployment requirement;
- account UI/auth-state contract;
- migration bridge from current token auth.

No external provider credentials are configured by this work package.
