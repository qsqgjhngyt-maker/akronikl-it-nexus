# CHANGELOG v0.1.4-alpha.2.1

- Fixed live OOP classification after a failed safe `override` compatibility retry.
- Browser Runtime capability failures now remain `provider-limit` warnings even when the second internal provider error is not parse-shaped.
- Provider-limit diagnostics no longer mark a learner source line as wrong.
- Technical details retain both primary and retry failures.
- Added a regression test reproducing the live GitHub Pages failure pattern.
- No benchmark content, stable IDs, legacy indices, or progress keys changed.
